version: '3.6'

services:
  db:
    build:
      context: ./Docker
      dockerfile: db.Dockerfile
    image: postgres
    restart: always
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=instabrand
      - POSTGRES_PASSWORD=WBezCcZ6x6peWBj7
      - POSTGRES_DB=instabrand-db
      
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

volumes:
  postgres: